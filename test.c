#include <stdio.h>
#include <conio.h>

void main()
{
	int r, c;
	int n;
	int i = 5, j = 1;
	printf("Enter n : ");
	scanf("%d", &n);
	for (r = 1; r <= (2 * n) - 1; r++)
	{
		for (c = 1; c <= (2 * n) - 1; c++)
		{
			printf("%d ", );
		}
		printf("\n");
	}
}